global ^

-- Fixes importing issue with Yuescript.
global require = _G['import']

import "macros" as $

$REQUIRE "bags"
$REQUIRE "randomizers"
$REQUIRE "randoms"
$REQUIRE "rotations"

playdate.update = ->