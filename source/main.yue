global ^

import "macros" as $

$IMPORT "macros"
$IMPORT "randomizers"
$IMPORT "rotations"

playdate.update = ->

playdate.keyPressed = (key) ->
playdate.keyReleased = (key) ->
playdate.debugDraw = ->